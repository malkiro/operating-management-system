package lk.bitproject.repository;

import lk.bitproject.model.CStatus;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CStatusRepository extends JpaRepository<CStatus, Integer> {
}
