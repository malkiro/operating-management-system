package lk.bitproject.repository;

import lk.bitproject.model.GrnBatch;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GrnBatchRepository extends JpaRepository<GrnBatch,Integer> {
}
