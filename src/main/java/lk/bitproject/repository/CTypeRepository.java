package lk.bitproject.repository;

import lk.bitproject.model.CType;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CTypeRepository extends JpaRepository<CType, Integer> {
}
